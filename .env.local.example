# 🧩 Convex + Clerk + Next.js Template
# Example environment variables (.env.local)
# Duplicate this file as `.env.local` and fill in your own values.

# ------------------------------------------------------------
# 🔐 Clerk (Authentication)
# ------------------------------------------------------------

# Your public key for Clerk Frontend SDK (safe to expose)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxx

# Your secret key for server-side verification (keep private!)
CLERK_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxx

# Webhook signing secret for verifying Clerk → Convex sync requests
CLERK_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxx

# Used by Convex auth.config.ts to validate JWTs (e.g. your Clerk instance domain)
# Example: https://your-app.clerk.accounts.dev
CLERK_JWT_ISSUER_DOMAIN=https://xxxxx.clerk.accounts.dev

# ------------------------------------------------------------
# ⚙️ Convex (Backend)
# ------------------------------------------------------------

# Convex deployment name ("dev" for local or the production slug from dashboard)
CONVEX_DEPLOYMENT=dev

# Convex URL for your frontend (usually auto-injected by Convex CLI)
NEXT_PUBLIC_CONVEX_URL=https://xxxx.convex.cloud
